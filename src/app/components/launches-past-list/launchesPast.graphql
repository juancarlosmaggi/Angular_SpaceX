query launchesPastList($limit: Int!, $offset: Int) {
  launchesPast(limit: $limit, offset: $offset) {
    id
    mission_name
    links {
      mission_patch_small
      video_link
    }
    rocket {
      rocket_name
    }
    launch_date_utc
    launch_success
  }
}
